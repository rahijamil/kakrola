import React from "react";

const TeamBillingSettingsPage = ({
  params: { team_id },
}: {
  params: { team_id: string };
}) => {
  return <div>TeamBillingSettingsPage</div>;
};

export default TeamBillingSettingsPage;
