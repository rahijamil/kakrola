import React from "react";

export const metadata = {
    title: "Threads - Kakrola",
};

const ThreadsLayout = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default ThreadsLayout;
